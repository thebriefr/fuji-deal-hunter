name: 📷 Fujifilm Deal Hunter

on:
  schedule:
    # Runs every hour. For 30-min intervals, change to: */30 * * * *
    # (30-min requires a public repo to stay within GitHub's free tier)
    - cron: "*/15 * * * *"
  workflow_dispatch:  # Allows manual trigger from the Actions tab

permissions:
  contents: write  # Needed to commit seen_listings.json back to repo

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ hashFiles('requirements.txt') }}
          restore-keys: pip-

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run deal scanner
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python scraper.py

      - name: Commit updated seen_listings.json
        run: |
          git config user.name  "Fuji Deal Hunter Bot"
          git config user.email "bot@noreply.github.com"
          git add seen_listings.json || true
          # Only commit if the file actually changed
          git diff --staged --quiet || git commit -m "chore: update seen listings [skip ci]"
          git push || true
